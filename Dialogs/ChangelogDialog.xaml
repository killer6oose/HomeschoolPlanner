<Window x:Class="HomeschoolPlanner.Dialogs.ChangelogDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="What's New"
        Width="480" SizeToContent="Height" MaxHeight="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource DialogBackgroundBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Accent header -->
        <Border Grid.Row="0" Background="{DynamicResource AccentBrush}" Padding="20,14">
            <StackPanel>
                <TextBlock Text="What's New" FontSize="17" FontWeight="Bold" Foreground="White" />
                <TextBlock x:Name="VersionLabel" FontSize="12" Foreground="White" Opacity="0.8" />
            </StackPanel>
        </Border>

        <!-- Scrollable changelog content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" MaxHeight="400">
            <RichTextBox x:Name="ChangelogBox"
                         IsReadOnly="True"
                         IsDocumentEnabled="True"
                         BorderThickness="0"
                         Background="Transparent"
                         Foreground="{DynamicResource TextPrimaryBrush}"
                         Padding="20,14,20,6" />
        </ScrollViewer>

        <!-- Footer -->
        <Border Grid.Row="2"
                BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,1,0,0"
                Padding="20,12" Background="{DynamicResource SurfaceBrush}">
            <Button Content="Got It"
                    Style="{DynamicResource PrimaryButtonStyle}"
                    HorizontalAlignment="Right"
                    MinWidth="80"
                    Click="GotIt_Click" />
        </Border>
    </Grid>
</Window>

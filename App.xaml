<Application x:Class="HomeschoolPlanner.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             StartupUri="MainWindow.xaml">
    <Application.Resources>

        <!-- Theme colors - updated at runtime by ThemeManager -->
        <SolidColorBrush x:Key="AccentBrush"         Color="#4A7CB5" />
        <SolidColorBrush x:Key="AccentHoverBrush"    Color="#3A6CA5" />
        <SolidColorBrush x:Key="BackgroundBrush"     Color="#F5F6FA" />
        <SolidColorBrush x:Key="SurfaceBrush"        Color="#FFFFFF" />
        <SolidColorBrush x:Key="BorderBrush"         Color="#D8DCE6" />
        <SolidColorBrush x:Key="TextPrimaryBrush"    Color="#1C2333" />
        <SolidColorBrush x:Key="TextSecondaryBrush"  Color="#6B7280" />
        <SolidColorBrush x:Key="CompletedGreenBrush" Color="#22C55E" />
        <!-- Hover/press brushes for buttons - computed per-theme by ThemeManager -->
        <SolidColorBrush x:Key="HoverBrush"          Color="#EEF2F8" />
        <SolidColorBrush x:Key="PressedBrush"        Color="#DDE4EF" />
        <!-- Danger Zone card colors - updated per-theme by ThemeManager -->
        <SolidColorBrush x:Key="DangerSurfaceBrush"  Color="#FFF5F5" />
        <SolidColorBrush x:Key="DangerBorderBrush"   Color="#FFCCCC" />
        <!-- Pride-specific resources - overwritten by ThemeManager.Apply() -->
        <SolidColorBrush x:Key="NavButtonBorderBrush" Color="#D8DCE6" />
        <Visibility x:Key="PrideGradientVisibility">Collapsed</Visibility>

        <!-- Font resources - updated at runtime by ThemeManager -->
        <FontFamily x:Key="AppFontFamily">Segoe UI</FontFamily>
        <sys:Double x:Key="AppFontSize">13</sys:Double>

        <!-- SVG icon geometries - used with Fill="{DynamicResource TextPrimaryBrush}" for theme-aware color.
             Canvas dimensions match each SVG's viewBox so Viewbox scales correctly. -->
        <Geometry x:Key="HelpIconGeometry">M12,2C6.5,2,2,6.5,2,12s4.5,10,10,10s10-4.5,10-10S17.5,2,12,2z M12,20c-4.5,0-8-3.5-8-8s3.5-8,8-8s8,3.5,8,8S16.5,20,12,20z M11,15H13V17H11z M13,14h-2c0-2.6,2-2.1,2-4.2c0-0.4-0.2-1.3-1.1-1.3c-0.3,0-1,0.5-1,1.5H9c0,0-0.4-3,2.9-3C14.5,7,15,8.9,15,9.8C15,12.1,13,12.3,13,14z</Geometry>
        <Geometry x:Key="ReportsIconGeometry">M31,31.36H1c-0.199,0-0.36-0.161-0.36-0.36V1c0-0.199,0.161-0.36,0.36-0.36h30c0.199,0,0.36,0.161,0.36,0.36v30C31.36,31.199,31.199,31.36,31,31.36z M1.36,30.64h29.28V12.36H1.36V30.64z M13.36,11.64h17.28V1.36H13.36V11.64z M1.36,11.64h11.28V1.36H1.36V11.64z M9,27.36c-2.956,0-5.36-2.405-5.36-5.36h0.72c0,2.559,2.082,4.64,4.64,4.64s4.64-2.081,4.64-4.64S11.559,17.36,9,17.36v-0.72c2.956,0,5.36,2.405,5.36,5.36S11.956,27.36,9,27.36z M27.36,27h-0.72V16h0.721L27.36,27z M23.36,27h-0.72v-8h0.721L23.36,27z M19.36,27h-0.72v-3h0.721L19.36,27z</Geometry>
        <!-- ResourcesIconGeometry is stroke-based: use Stroke="{DynamicResource TextPrimaryBrush}" Fill="Transparent" -->
        <Geometry x:Key="ResourcesIconGeometry">M9,25.44a0.9,0.9,0,0,0,-0.9,0.91V42.6a0.9,0.9,0,0,0,0.9,0.9H21.65a0.9,0.9,0,0,0,0.91,-0.9V26.35a0.91,0.91,0,0,0,-0.91,-0.91Z M9,4.5a0.9,0.9,0,0,0,-0.9,0.9V21.65a0.9,0.9,0,0,0,0.9,0.91H21.65a0.91,0.91,0,0,0,0.91,-0.91V5.4a0.9,0.9,0,0,0,-0.91,-0.9H20.56V10.5L18.56,8.5L16.56,10.5V4.5Z M26.35,25.44a0.91,0.91,0,0,0,-0.91,0.91V42.6a0.9,0.9,0,0,0,0.91,0.9H39a0.9,0.9,0,0,0,0.9,-0.9V26.35a0.9,0.9,0,0,0,-0.9,-0.91H37.9V31.35L35.9,29.35L33.9,31.35V25.44Z M26.35,4.5a0.9,0.9,0,0,0,-0.91,0.9V21.65a0.91,0.91,0,0,0,0.91,0.91H39a0.9,0.9,0,0,0,0.9,-0.91V5.4a0.9,0.9,0,0,0,-0.9,-0.9H37.9V9.83H33.9V4.5Z M11.35,14.66H19.32 M19.32,18.11H11.35 M28.68,14.66H36.65 M36.65,18.11H28.68 M36.65,34.47H28.68 M19.32,34.47H11.35 M19.32,31.02H11.35 M19.32,37.92H11.35</Geometry>
        <Geometry x:Key="SchoolIconGeometry">M503.5,476H487V267h16.5c2.737,0,5.257-1.491,6.575-3.891c1.317-2.4,1.222-5.327-0.247-7.636l-56-88c-1.377-2.164-3.763-3.474-6.328-3.474H298.606L263,128.394V99h72.5c2.766,0,5.308-1.522,6.613-3.961c1.305-2.438,1.162-5.398-0.373-7.699L328.514,67.5l13.227-19.84c1.535-2.301,1.677-5.261,0.373-7.699C340.808,37.522,338.266,36,335.5,36H263v-8.5c0-4.142-3.358-7.5-7.5-7.5s-7.5,3.358-7.5,7.5v100.894L212.394,164H63.5c-2.564,0-4.951,1.31-6.328,3.474l-56,88c-1.469,2.309-1.564,5.236-0.247,7.636C2.243,265.509,4.763,267,7.5,267H24v209H7.5c-4.142,0-7.5,3.358-7.5,7.5s3.358,7.5,7.5,7.5h496c4.142,0,7.5-3.358,7.5-7.5S507.642,476,503.5,476z M443.383,179l46.455,73H386.606l-73-73H443.383z M321.486,51l-8.227,12.34c-1.68,2.519-1.68,5.801,0,8.32L321.486,84H263V51H321.486z M67.617,179h129.776l-73,73H21.163L67.617,179z M39,267h88.498c0.248,0,0.496-0.013,0.744-0.038c0.248-0.025,0.494-0.063,0.735-0.116c0.235-0.051,0.467-0.117,0.694-0.199c0.222-0.08,0.438-0.175,0.647-0.284c0.206-0.108,0.403-0.23,0.592-0.365c0.183-0.131,0.355-0.275,0.516-0.43L255.5,142.106l122.696,122.695c0.161,0.155,0.333,0.299,0.516,0.43c0.189,0.135,0.386,0.257,0.592,0.365c0.209,0.109,0.425,0.204,0.647,0.284c0.227,0.082,0.459,0.148,0.694,0.199c0.241,0.053,0.487,0.091,0.735,0.116c0.248,0.025,0.496,0.038,0.744,0.038H472v209H311V371.5c0-12.958-10.542-23.5-23.5-23.5h-64c-12.958,0-23.5,10.542-23.5,23.5V476H39V267z M296,380h-81v-8.5c0-4.687,3.813-8.5,8.5-8.5h64c4.687,0,8.5,3.813,8.5,8.5V380z M215,395h33v81h-33V395z M263,395h33v81h-33V395z M255.5,323c30.603,0,55.5-24.897,55.5-55.5S286.103,212,255.5,212S200,236.897,200,267.5S224.897,323,255.5,323z M255.5,227c22.332,0,40.5,18.168,40.5,40.5S277.832,308,255.5,308S215,289.832,215,267.5S233.168,227,255.5,227z M255.5,275c4.142,0,7.5-3.358,7.5-7.5v-24c0-4.142-3.358-7.5-7.5-7.5s-7.5,3.358-7.5,7.5V260h-16.5c-4.142,0-7.5,3.358-7.5,7.5s3.358,7.5,7.5,7.5H255.5z M175.5,388h-40c-4.142,0-7.5,3.358-7.5,7.5v48c0,4.142,3.358,7.5,7.5,7.5h40c4.142,0,7.5-3.358,7.5-7.5v-48C183,391.358,179.642,388,175.5,388z M168,403v9h-25v-9H168z M143,436v-9h25v9H143z M103.5,388h-40c-4.142,0-7.5,3.358-7.5,7.5v48c0,4.142,3.358,7.5,7.5,7.5h40c4.142,0,7.5-3.358,7.5-7.5v-48C111,391.358,107.642,388,103.5,388z M96,403v9H71v-9H96z M71,436v-9h25v9H71z M173,292h-35c-5.514,0-10,4.486-10,10v53.5c0,4.142,3.358,7.5,7.5,7.5h40c4.142,0,7.5-3.358,7.5-7.5V302C183,296.486,178.514,292,173,292z M168,307v17h-25v-17H168z M143,348v-9h25v9H143z M101,292H66c-5.514,0-10,4.486-10,10v53.5c0,4.142,3.358,7.5,7.5,7.5h40c4.142,0,7.5-3.358,7.5-7.5V302C111,296.486,106.514,292,101,292z M96,307v17H71v-17H96z M71,348v-9h25v9H71z M407.5,451h40c4.142,0,7.5-3.358,7.5-7.5v-48c0-4.142-3.358-7.5-7.5-7.5h-40c-4.142,0-7.5,3.358-7.5,7.5v48C400,447.642,403.358,451,407.5,451z M415,436v-9h25v9H415z M440,403v9h-25v-9H440z M335.5,451h40c4.142,0,7.5-3.358,7.5-7.5v-48c0-4.142-3.358-7.5-7.5-7.5h-40c-4.142,0-7.5,3.358-7.5,7.5v48C328,447.642,331.358,451,335.5,451z M343,436v-9h25v9H343z M368,403v9h-25v-9H368z M407.5,363h40c4.142,0,7.5-3.358,7.5-7.5V302c0-5.514-4.486-10-10-10h-35c-5.514,0-10,4.486-10,10v53.5C400,359.642,403.358,363,407.5,363z M415,348v-9h25v9H415z M440,307v17h-25v-17H440z M335.5,363h40c4.142,0,7.5-3.358,7.5-7.5V302c0-5.514-4.486-10-10-10h-35c-5.514,0-10,4.486-10,10v53.5C328,359.642,331.358,363,335.5,363z M343,348v-9h25v9H343z M368,307v17h-25v-17H368z</Geometry>

        <!-- Global text style -->
        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="{DynamicResource AppFontFamily}" />
            <Setter Property="FontSize"   Value="{DynamicResource AppFontSize}" />
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
        </Style>

        <!-- TextBox - background and text from theme -->
        <Style TargetType="TextBox">
            <Setter Property="FontFamily"   Value="{DynamicResource AppFontFamily}" />
            <Setter Property="FontSize"     Value="{DynamicResource AppFontSize}" />
            <Setter Property="Padding"      Value="6,5" />
            <Setter Property="Background"   Value="{DynamicResource SurfaceBrush}" />
            <Setter Property="Foreground"   Value="{DynamicResource TextPrimaryBrush}" />
            <Setter Property="BorderBrush"  Value="{DynamicResource BorderBrush}" />
            <Setter Property="CaretBrush"   Value="{DynamicResource TextPrimaryBrush}" />
        </Style>

        <!-- ComboBox - modern flat style with rounded border, chevron arrow, accent focus ring -->
        <Style TargetType="ComboBox">
            <Setter Property="FontFamily"               Value="{DynamicResource AppFontFamily}" />
            <Setter Property="FontSize"                 Value="{DynamicResource AppFontSize}" />
            <Setter Property="Foreground"               Value="{DynamicResource TextPrimaryBrush}" />
            <Setter Property="Background"               Value="{DynamicResource SurfaceBrush}" />
            <Setter Property="BorderBrush"              Value="{DynamicResource BorderBrush}" />
            <Setter Property="BorderThickness"          Value="1" />
            <Setter Property="Height"                   Value="34" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Cursor"                   Value="Hand" />
            <Setter Property="MaxDropDownHeight"        Value="220" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <!-- Toggle button provides the border/background and handles open/close clicks -->
                            <ToggleButton x:Name="ToggleBtn"
                                          IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                                          Focusable="False"
                                          ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="Bd"
                                                Background="{DynamicResource SurfaceBrush}"
                                                BorderBrush="{DynamicResource BorderBrush}"
                                                BorderThickness="1"
                                                CornerRadius="6">
                                            <!-- Chevron arrow, right-aligned -->
                                            <Path x:Name="Arrow"
                                                  Data="M 0 0 L 4 4 L 8 0"
                                                  Stroke="{DynamicResource TextSecondaryBrush}"
                                                  StrokeThickness="1.5"
                                                  StrokeLineJoin="Round"
                                                  StrokeStartLineCap="Round"
                                                  StrokeEndLineCap="Round"
                                                  Fill="Transparent"
                                                  Width="8" Height="4"
                                                  HorizontalAlignment="Right"
                                                  VerticalAlignment="Center"
                                                  Margin="0,0,10,0"
                                                  RenderTransformOrigin="0.5,0.5">
                                                <Path.RenderTransform>
                                                    <RotateTransform x:Name="ArrowRotate" Angle="0" />
                                                </Path.RenderTransform>
                                            </Path>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                                            </Trigger>
                                            <!-- Flip the chevron when open -->
                                            <Trigger Property="IsChecked" Value="True">
                                                <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                                                <Trigger.EnterActions>
                                                    <BeginStoryboard>
                                                        <Storyboard>
                                                            <DoubleAnimation Storyboard.TargetName="ArrowRotate"
                                                                             Storyboard.TargetProperty="Angle"
                                                                             To="180" Duration="0:0:0.15" />
                                                        </Storyboard>
                                                    </BeginStoryboard>
                                                </Trigger.EnterActions>
                                                <Trigger.ExitActions>
                                                    <BeginStoryboard>
                                                        <Storyboard>
                                                            <DoubleAnimation Storyboard.TargetName="ArrowRotate"
                                                                             Storyboard.TargetProperty="Angle"
                                                                             To="0" Duration="0:0:0.15" />
                                                        </Storyboard>
                                                    </BeginStoryboard>
                                                </Trigger.ExitActions>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>

                            <!-- Selected item display - floats on top, passes clicks through to ToggleButton -->
                            <ContentPresenter x:Name="ContentSite"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                              Margin="10,0,32,0"
                                              VerticalAlignment="Center"
                                              IsHitTestVisible="False" />

                            <!-- Dropdown popup -->
                            <Popup x:Name="PART_Popup"
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Fade">
                                <Border Background="{DynamicResource SurfaceBrush}"
                                        BorderBrush="{DynamicResource BorderBrush}"
                                        BorderThickness="1"
                                        CornerRadius="6"
                                        Margin="0,3,0,0"
                                        MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}">
                                    <Border.Effect>
                                        <DropShadowEffect Color="#000000" Opacity="0.1" BlurRadius="14" ShadowDepth="4" Direction="270" />
                                    </Border.Effect>
                                    <ScrollViewer MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                                  VerticalScrollBarVisibility="Auto"
                                                  HorizontalScrollBarVisibility="Disabled"
                                                  Padding="4,4">
                                        <ItemsPresenter />
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ContentSite" Property="Opacity" Value="0.45" />
                                <Setter TargetName="ToggleBtn"   Property="Opacity" Value="0.45" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBoxItem - rounded hover/selected states -->
        <Style TargetType="ComboBoxItem">
            <Setter Property="FontFamily"  Value="{DynamicResource AppFontFamily}" />
            <Setter Property="FontSize"    Value="{DynamicResource AppFontSize}" />
            <Setter Property="Foreground"  Value="{DynamicResource TextPrimaryBrush}" />
            <Setter Property="Background"  Value="Transparent" />
            <Setter Property="Padding"     Value="10,7" />
            <Setter Property="Cursor"      Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <!-- OuterBd handles shape/clipping; layers inside handle the background -->
                        <Border x:Name="OuterBd"
                                CornerRadius="4"
                                Margin="3,1"
                                ClipToBounds="True">
                            <Grid>
                                <!-- Solid accent bg (non-Pride selected state) -->
                                <Border x:Name="AccentBd" Background="Transparent" />
                                <!-- Pride rainbow overlay - Collapsed in non-Pride themes via DynamicResource.
                                     Opacity=0 so it's invisible until an item is selected. -->
                                <Border x:Name="GradientBd"
                                        Opacity="0"
                                        Visibility="{DynamicResource PrideGradientVisibility}">
                                    <Border.Background>
                                        <LinearGradientBrush StartPoint="0,0.5" EndPoint="1,0.5">
                                            <GradientStop Color="#E05050" Offset="0.000" />
                                            <GradientStop Color="#D4832A" Offset="0.167" />
                                            <GradientStop Color="#A8A828" Offset="0.333" />
                                            <GradientStop Color="#28A060" Offset="0.500" />
                                            <GradientStop Color="#2864D0" Offset="0.667" />
                                            <GradientStop Color="#8C28C8" Offset="0.833" />
                                            <GradientStop Color="#E05050" Offset="1.000" />
                                        </LinearGradientBrush>
                                    </Border.Background>
                                </Border>
                                <ContentPresenter Margin="{TemplateBinding Padding}" />
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="AccentBd" Property="Background" Value="{DynamicResource HoverBrush}" />
                                <!-- In Pride theme, show a dim rainbow tint on hover (GradientBd is Collapsed in other themes) -->
                                <Setter TargetName="GradientBd" Property="Opacity" Value="0.25" />
                            </Trigger>
                            <!-- Selected: solid accent at 70% (non-Pride), rainbow at 75% (Pride) -->
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="AccentBd" Property="Background" Value="{DynamicResource AccentBrush}" />
                                <Setter TargetName="AccentBd" Property="Opacity" Value="0.7" />
                                <Setter TargetName="GradientBd" Property="Opacity" Value="0.75" />
                                <Setter Property="Foreground" Value="White" />
                            </Trigger>
                            <!-- Selected + hover: nudge both layers slightly brighter -->
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsSelected"  Value="True" />
                                    <Condition Property="IsMouseOver" Value="True" />
                                </MultiTrigger.Conditions>
                                <Setter TargetName="AccentBd" Property="Background" Value="{DynamicResource AccentBrush}" />
                                <Setter TargetName="AccentBd" Property="Opacity" Value="0.85" />
                                <Setter TargetName="GradientBd" Property="Opacity" Value="0.9" />
                                <Setter Property="Foreground" Value="White" />
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- TabControl - background and border from theme -->
        <Style TargetType="TabControl">
            <Setter Property="Background"   Value="{DynamicResource SurfaceBrush}" />
            <Setter Property="BorderBrush"  Value="{DynamicResource BorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding"      Value="4" />
            <Setter Property="FontFamily"   Value="{DynamicResource AppFontFamily}" />
            <Setter Property="FontSize"     Value="{DynamicResource AppFontSize}" />
        </Style>

        <!-- TabItem - themed header text, selected vs unselected states -->
        <Style TargetType="TabItem">
            <Setter Property="FontFamily"   Value="{DynamicResource AppFontFamily}" />
            <Setter Property="FontSize"     Value="{DynamicResource AppFontSize}" />
            <Setter Property="Foreground"   Value="{DynamicResource TextSecondaryBrush}" />
            <Setter Property="Background"   Value="Transparent" />
            <Setter Property="BorderBrush"  Value="{DynamicResource BorderBrush}" />
            <Setter Property="Padding"      Value="12,6" />
            <Setter Property="Cursor"       Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="TabBd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{DynamicResource BorderBrush}"
                                BorderThickness="1,1,1,0"
                                CornerRadius="4,4,0,0"
                                Padding="{TemplateBinding Padding}"
                                Margin="0,0,2,0"
                                SnapsToDevicePixels="True">
                            <ContentPresenter x:Name="TabContent"
                                              ContentSource="Header"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              TextBlock.Foreground="{DynamicResource TextSecondaryBrush}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="TabBd"      Property="Background"   Value="{DynamicResource HoverBrush}" />
                                <Setter TargetName="TabContent" Property="TextBlock.Foreground" Value="{DynamicResource TextPrimaryBrush}" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="TabBd"      Property="Background"   Value="{DynamicResource SurfaceBrush}" />
                                <Setter TargetName="TabBd"      Property="BorderBrush"  Value="{DynamicResource AccentBrush}" />
                                <Setter TargetName="TabContent" Property="TextBlock.Foreground" Value="{DynamicResource TextPrimaryBrush}" />
                                <Setter Property="FontWeight" Value="SemiBold" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- DatePicker - field colors from theme -->
        <Style TargetType="DatePicker">
            <Setter Property="FontFamily"   Value="{DynamicResource AppFontFamily}" />
            <Setter Property="FontSize"     Value="{DynamicResource AppFontSize}" />
            <Setter Property="Foreground"   Value="{DynamicResource TextPrimaryBrush}" />
            <Setter Property="Background"   Value="{DynamicResource SurfaceBrush}" />
            <Setter Property="BorderBrush"  Value="{DynamicResource BorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding"      Value="4,3" />
            <Setter Property="IsTodayHighlighted" Value="True" />
        </Style>

        <!-- DatePickerTextBox - the editable text part inside DatePicker -->
        <Style TargetType="DatePickerTextBox">
            <Setter Property="FontFamily"   Value="{DynamicResource AppFontFamily}" />
            <Setter Property="FontSize"     Value="{DynamicResource AppFontSize}" />
            <Setter Property="Foreground"   Value="{DynamicResource TextPrimaryBrush}" />
            <Setter Property="Background"   Value="{DynamicResource SurfaceBrush}" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding"      Value="2,0" />
            <Setter Property="SelectionBrush" Value="{DynamicResource AccentBrush}" />
            <Setter Property="CaretBrush"   Value="{DynamicResource TextPrimaryBrush}" />
        </Style>

        <!-- Calendar popup - background and borders from theme -->
        <Style TargetType="Calendar">
            <Setter Property="Background"   Value="{DynamicResource SurfaceBrush}" />
            <Setter Property="BorderBrush"  Value="{DynamicResource BorderBrush}" />
            <Setter Property="Foreground"   Value="{DynamicResource TextPrimaryBrush}" />
            <Setter Property="FontFamily"   Value="{DynamicResource AppFontFamily}" />
            <Setter Property="FontSize"     Value="{DynamicResource AppFontSize}" />
        </Style>

        <!-- CalendarDayButton - individual day cells in the calendar popup -->
        <Style TargetType="CalendarDayButton">
            <Setter Property="FontFamily"   Value="{DynamicResource AppFontFamily}" />
            <Setter Property="FontSize"     Value="{DynamicResource AppFontSize}" />
            <Setter Property="Foreground"   Value="{DynamicResource TextPrimaryBrush}" />
            <Setter Property="Background"   Value="Transparent" />
            <Setter Property="MinWidth"     Value="34" />
            <Setter Property="MinHeight"    Value="28" />
        </Style>

        <!-- CalendarButton - month/year navigation cells -->
        <Style TargetType="CalendarButton">
            <Setter Property="FontFamily"   Value="{DynamicResource AppFontFamily}" />
            <Setter Property="FontSize"     Value="{DynamicResource AppFontSize}" />
            <Setter Property="Foreground"   Value="{DynamicResource TextPrimaryBrush}" />
            <Setter Property="Background"   Value="Transparent" />
            <Setter Property="MinWidth"     Value="40" />
            <Setter Property="MinHeight"    Value="42" />
        </Style>

        <!-- CheckBox -->
        <Style TargetType="CheckBox">
            <Setter Property="FontFamily"  Value="{DynamicResource AppFontFamily}" />
            <Setter Property="FontSize"    Value="{DynamicResource AppFontSize}" />
            <Setter Property="Foreground"  Value="{DynamicResource TextPrimaryBrush}" />
        </Style>

        <!-- RadioButton -->
        <Style TargetType="RadioButton">
            <Setter Property="FontFamily"  Value="{DynamicResource AppFontFamily}" />
            <Setter Property="FontSize"    Value="{DynamicResource AppFontSize}" />
            <Setter Property="Foreground"  Value="{DynamicResource TextPrimaryBrush}" />
        </Style>

        <!-- ListBox -->
        <Style TargetType="ListBox">
            <Setter Property="FontFamily"  Value="{DynamicResource AppFontFamily}" />
            <Setter Property="FontSize"    Value="{DynamicResource AppFontSize}" />
            <Setter Property="Background"  Value="{DynamicResource SurfaceBrush}" />
            <Setter Property="Foreground"  Value="{DynamicResource TextPrimaryBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
        </Style>

        <!-- ListBoxItem -->
        <Style TargetType="ListBoxItem">
            <Setter Property="FontFamily"  Value="{DynamicResource AppFontFamily}" />
            <Setter Property="FontSize"    Value="{DynamicResource AppFontSize}" />
            <Setter Property="Background"  Value="{DynamicResource SurfaceBrush}" />
            <Setter Property="Foreground"  Value="{DynamicResource TextPrimaryBrush}" />
            <Setter Property="Padding"     Value="6,4" />
        </Style>

        <!-- Nav/outline button.
             In Pride theme ThemeManager sets NavButtonBorderBrush to a rainbow LinearGradientBrush,
             giving every secondary button a rainbow outline automatically. -->
        <Style x:Key="NavButtonStyle" TargetType="Button">
            <Setter Property="Background"      Value="Transparent" />
            <Setter Property="Foreground"      Value="{DynamicResource TextPrimaryBrush}" />
            <Setter Property="BorderBrush"     Value="{DynamicResource NavButtonBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding"         Value="12,6" />
            <Setter Property="FontSize"        Value="{DynamicResource AppFontSize}" />
            <Setter Property="FontFamily"      Value="{DynamicResource AppFontFamily}" />
            <Setter Property="Cursor"          Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource HoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource PressedBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Active view toggle -->
        <Style x:Key="ViewToggleActiveStyle" TargetType="Button" BasedOn="{StaticResource NavButtonStyle}">
            <Setter Property="Background"  Value="{DynamicResource AccentBrush}" />
            <Setter Property="Foreground"  Value="White" />
            <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource AccentHoverBrush}" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Primary filled button.
             In Pride theme a 200%-wide rainbow gradient (GradientBd) overlays the solid accent.
             On hover it slides from showing the warm left half to the cool right half,
             matching the CronoTech.org button effect. GradientBd is Collapsed for non-pride themes
             so non-pride buttons behave normally and the animations are invisible. -->
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background"      Value="{DynamicResource AccentBrush}" />
            <Setter Property="Foreground"      Value="White" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding"         Value="14,7" />
            <Setter Property="FontSize"        Value="{DynamicResource AppFontSize}" />
            <Setter Property="FontFamily"      Value="{DynamicResource AppFontFamily}" />
            <Setter Property="Cursor"          Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <!-- Solid accent background (all themes) -->
                            <Border x:Name="SolidBd"
                                    Background="{TemplateBinding Background}"
                                    CornerRadius="4" />
                            <!-- Pride rainbow overlay: spans 200% width so StartPoint/EndPoint
                                 animation reveals left half (warm) then right half (cool) on hover.
                                 Collapsed in non-pride themes - animations still fire but are invisible. -->
                            <Border x:Name="GradientBd"
                                    Visibility="{DynamicResource PrideGradientVisibility}"
                                    CornerRadius="4">
                                <Border.Background>
                                    <LinearGradientBrush StartPoint="0,0.5" EndPoint="2,0.5">
                                        <GradientStop Color="#E05050" Offset="0.000" />
                                        <GradientStop Color="#D4832A" Offset="0.167" />
                                        <GradientStop Color="#A8A828" Offset="0.333" />
                                        <GradientStop Color="#28A060" Offset="0.500" />
                                        <GradientStop Color="#2864D0" Offset="0.667" />
                                        <GradientStop Color="#8C28C8" Offset="0.833" />
                                        <GradientStop Color="#E05050" Offset="1.000" />
                                    </LinearGradientBrush>
                                </Border.Background>
                            </Border>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}" />
                        </Grid>
                        <ControlTemplate.Triggers>
                            <!-- Non-pride hover/press: dim the solid bg -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="SolidBd" Property="Background" Value="{DynamicResource AccentHoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="SolidBd" Property="Background" Value="{DynamicResource PressedBrush}" />
                            </Trigger>
                            <!-- Pride: slide gradient right on enter (warm -> cool half) -->
                            <EventTrigger RoutedEvent="MouseEnter">
                                <BeginStoryboard HandoffBehavior="SnapshotAndReplace">
                                    <Storyboard>
                                        <PointAnimation Storyboard.TargetName="GradientBd"
                                                        Storyboard.TargetProperty="Background.StartPoint"
                                                        To="-1,0.5" Duration="0:0:0.4" />
                                        <PointAnimation Storyboard.TargetName="GradientBd"
                                                        Storyboard.TargetProperty="Background.EndPoint"
                                                        To="1,0.5" Duration="0:0:0.4" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                            <!-- Pride: slide back to warm half on leave -->
                            <EventTrigger RoutedEvent="MouseLeave">
                                <BeginStoryboard HandoffBehavior="SnapshotAndReplace">
                                    <Storyboard FillBehavior="Stop">
                                        <PointAnimation Storyboard.TargetName="GradientBd"
                                                        Storyboard.TargetProperty="Background.StartPoint"
                                                        To="0,0.5" Duration="0:0:0.3" />
                                        <PointAnimation Storyboard.TargetName="GradientBd"
                                                        Storyboard.TargetProperty="Background.EndPoint"
                                                        To="2,0.5" Duration="0:0:0.3" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Lesson cell (clickable grid cell in WeekView) -->
        <Style x:Key="LessonCellStyle" TargetType="Button">
            <Setter Property="Background"                 Value="{DynamicResource SurfaceBrush}" />
            <Setter Property="BorderBrush"                Value="{DynamicResource BorderBrush}" />
            <Setter Property="BorderThickness"            Value="0,0,1,1" />
            <Setter Property="Padding"                    Value="6,4" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="VerticalContentAlignment"   Value="Top" />
            <Setter Property="Cursor"                     Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource HoverBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ═══════════════════════════════════════════════════════════════
             Menu + MenuItem — fully themed so system Aero2 colors don't
             bleed through. Handles TopLevelHeader, SubmenuItem, and
             SubmenuHeader roles; IsHighlighted uses HoverBrush so every
             theme (including Pride Dark) is readable.
             ═══════════════════════════════════════════════════════════════ -->

        <DropShadowEffect x:Key="MenuPopupShadow"
                          BlurRadius="10" ShadowDepth="3"
                          Opacity="0.25" Color="Black" />

        <!-- Separator line inside menu popups -->
        <Style x:Key="MenuSeparatorStyle" TargetType="Separator">
            <Setter Property="Height"     Value="1" />
            <Setter Property="Margin"     Value="8,3" />
            <Setter Property="Background" Value="{DynamicResource BorderBrush}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Separator">
                        <Rectangle Height="1"
                                   Fill="{TemplateBinding Background}"
                                   Margin="{TemplateBinding Margin}" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Menu">
            <Setter Property="Background"       Value="{DynamicResource SurfaceBrush}" />
            <Setter Property="Foreground"       Value="{DynamicResource TextPrimaryBrush}" />
            <Setter Property="BorderThickness"  Value="0" />
            <Setter Property="FontFamily"       Value="{DynamicResource AppFontFamily}" />
            <Setter Property="FontSize"         Value="{DynamicResource AppFontSize}" />
        </Style>

        <Style TargetType="MenuItem">
            <Setter Property="Background"      Value="Transparent" />
            <Setter Property="Foreground"      Value="{DynamicResource TextPrimaryBrush}" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="FontFamily"      Value="{DynamicResource AppFontFamily}" />
            <Setter Property="FontSize"        Value="{DynamicResource AppFontSize}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <Grid SnapsToDevicePixels="True">

                            <!-- Hit-test background + hover highlight -->
                            <Border x:Name="Bd"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    Padding="{TemplateBinding Padding}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" SharedSizeGroup="MenuIconCol" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" SharedSizeGroup="MenuShortcutCol" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                    <!-- Icon (hidden for top-level bar items) -->
                                    <ContentPresenter x:Name="Icon"
                                                      Grid.Column="0"
                                                      ContentSource="Icon"
                                                      Width="16" Height="16"
                                                      Margin="0,0,8,0"
                                                      HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"
                                                      SnapsToDevicePixels="True" />

                                    <!-- Label -->
                                    <ContentPresenter Grid.Column="1"
                                                      ContentSource="Header"
                                                      RecognizesAccessKey="True"
                                                      VerticalAlignment="Center"
                                                      SnapsToDevicePixels="True" />

                                    <!-- Keyboard shortcut hint -->
                                    <TextBlock x:Name="IGT"
                                               Grid.Column="2"
                                               Text="{TemplateBinding InputGestureText}"
                                               Margin="14,0,0,0"
                                               VerticalAlignment="Center"
                                               Foreground="{DynamicResource TextSecondaryBrush}" />

                                    <!-- Submenu arrow -->
                                    <Path x:Name="Arrow"
                                          Grid.Column="3"
                                          Data="M 0 0 L 4 4 L 0 8 Z"
                                          Fill="{DynamicResource TextSecondaryBrush}"
                                          Margin="8,0,0,0"
                                          VerticalAlignment="Center"
                                          Visibility="Collapsed" />
                                </Grid>
                            </Border>

                            <!-- Popup (submenu / top-level dropdown) -->
                            <Popup x:Name="PART_Popup"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   IsOpen="{TemplateBinding IsSubmenuOpen}"
                                   Placement="Bottom"
                                   VerticalOffset="1"
                                   PopupAnimation="{DynamicResource {x:Static SystemParameters.MenuPopupAnimationKey}}">
                                <Border x:Name="PopupBd"
                                        Background="{DynamicResource SurfaceBrush}"
                                        BorderBrush="{DynamicResource BorderBrush}"
                                        BorderThickness="1"
                                        Padding="0,4"
                                        SnapsToDevicePixels="True"
                                        Effect="{StaticResource MenuPopupShadow}">
                                    <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Cycle"
                                                    SnapsToDevicePixels="True"
                                                    Grid.IsSharedSizeScope="True" />
                                </Border>
                            </Popup>

                        </Grid>
                        <ControlTemplate.Triggers>

                            <!-- Role: top-level header (File, Settings…) -->
                            <Trigger Property="Role" Value="TopLevelHeader">
                                <Setter Property="Padding"                    Value="10,6" />
                                <Setter TargetName="Icon" Property="Visibility" Value="Collapsed" />
                                <Setter TargetName="IGT"  Property="Visibility" Value="Collapsed" />
                            </Trigger>

                            <!-- Role: top-level item (no children, rare) -->
                            <Trigger Property="Role" Value="TopLevelItem">
                                <Setter Property="Padding"                    Value="10,6" />
                                <Setter TargetName="Icon" Property="Visibility" Value="Collapsed" />
                            </Trigger>

                            <!-- Role: popup item -->
                            <Trigger Property="Role" Value="SubmenuItem">
                                <Setter Property="Padding" Value="8,6,20,6" />
                            </Trigger>

                            <!-- Role: popup header with children -->
                            <Trigger Property="Role" Value="SubmenuHeader">
                                <Setter Property="Padding"                       Value="8,6,20,6" />
                                <Setter TargetName="Arrow"      Property="Visibility" Value="Visible" />
                                <Setter TargetName="PART_Popup" Property="Placement" Value="Right" />
                            </Trigger>

                            <!-- Hover / keyboard highlight -->
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource HoverBrush}" />
                            </Trigger>

                            <!-- Top-level item open (shows dropdown) -->
                            <Trigger Property="IsSubmenuOpen" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource HoverBrush}" />
                            </Trigger>

                            <!-- Disabled -->
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}" />
                            </Trigger>

                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Application.Resources>
</Application>
